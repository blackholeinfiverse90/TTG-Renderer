services:
  - type: web
    name: ttg-engine
    env: python
    buildCommand: pip install -r requirements.txt && npm install
    startCommand: bash start.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    autoDeploy: true
